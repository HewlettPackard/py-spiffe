"""PySPIFFE is a Python library for SPIFFE.

Basic usage:
TODO: add usage example here

   >>> import pyspiffe
   >>>
   >>>

"""
import logging
from logging import NullHandler

logging.getLogger(__name__).addHandler(NullHandler())
